# ===========================================
# DOCKER ENVIRONMENT CONFIGURATION
# Copy file này thành .env và điền giá trị
# ===========================================

# -----------------------------
# DATABASE (SQL Server)
# -----------------------------
# Password phải mạnh: chữ hoa + chữ thường + số + ký tự đặc biệt, ít nhất 8 ký tự
# Ví dụ: MyP@ssw0rd!, Admin@2024
DB_PASSWORD=

# -----------------------------
# JWT AUTHENTICATION
# -----------------------------
# Chuỗi bí mật để mã hóa token, ít nhất 32 ký tự
# Có thể dùng random string generator
JWT_KEY=

# -----------------------------
# EMAIL (SMTP - Tùy chọn)
# -----------------------------
# Để gửi email thông báo, xác nhận,...
# Nếu dùng Gmail: bật 2FA và tạo App Password
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
EMAIL_FROM=
EMAIL_PASSWORD=

# -----------------------------
# CORS (Tùy chọn)
# -----------------------------
# URL frontend được phép truy cập API
# Mặc định đã cấu hình trong docker-compose
FRONTEND_URL=http://localhost:5173

# -----------------------------
# PORTS (Tùy chọn)
# -----------------------------
# Đổi nếu port mặc định bị trùng
API_PORT=5000
FRONTEND_PORT=5173
SQL_PORT=1433
REDIS_PORT=6379

